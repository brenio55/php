Para conectar ao banco de dados, precisamos de algumas restrições de segurança.

Para usar o banco de dados por meio do PHP, a classe msqli, não existe mais. Agora usa-se duas formas:

    A classe MySQLi, e o PDO.

ini_set('display_errors', 1); 

ini_set('display_startup_errors', 1); 

error_reporting(E_ALL);

Use estas linhas de comando quando o PHP usando o MySQL não exibir a mensagem de erro e exibir o erro 500.